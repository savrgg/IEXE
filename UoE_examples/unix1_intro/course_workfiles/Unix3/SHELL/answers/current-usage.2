#!/bin/sh

# set positional paramteres to quota values
# $2 is usage
# $3 is quota
# $4 is limit
set `quota -v | sed 1,3d `

# calculate available disk as quota - usage
Available=`expr $3 - $2`

echo Your current usage is $2, $Available available
exit 0
